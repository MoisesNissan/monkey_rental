<div class="position-relative">
  <div class="card-category card-large" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)) %>">
  <%# , url(<%= @monkey.photo.attached? ? cl_image_path(@monkey.photo.key) : asset_path('monkey_background.jpg') )%>
    <h1><%= @monkey.name %></h1>
  </div>
</div>
<div class="container mb-5">
  <div class="d-flex justify-content-between my-4">
    <div>
        Add a monkey
    </div>
  </div>
  <div class="row my-2">
    <% @monkeys.each do |monkey| %>
      <div class="col-12 col-sm-6 mb-4" data-aos="zoom-in">
        <div class="bg-white rounded shadow h-100">
          <div class="p-3 rounded-bottom d-flex flex-column">
            <strong><%= monkey.name %> <span class="text-muted font-weight-normal">•
            <div class="d-flex align-items-end justify-content-between pt-4">
              <div>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
